// src/types/window.d.ts
/*
import type * as fs from "@tauri-apps/plugin-fs";

declare global {
  interface Window {
    tauri: {
      fs: {
        readTextFile: typeof fs.readTextFile;
        writeFile: typeof fs.writeFile;
        readFile: typeof fs.readFile;
        BaseDirectory: typeof fs.BaseDirectory;
      };
    };
  }
}

export {};
*/
